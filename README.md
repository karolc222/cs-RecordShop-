ASP.NET Core Web API for managing a record shop system.

## Overview

This project is a RESTful API built using ASP.NET Core. It allows users to manage records (e.g. albums, artists) and demonstrates backend development concepts such as controllers, services, and database integration.